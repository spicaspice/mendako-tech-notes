# 403エラーってなんだっけ。他のエラーについても

## 概要

`403` は HTTP ステータスコードのひとつで、ざっくり言うと「そのページや API にはアクセスできない」ときに返るエラーです。Web のエラー番号は 3 桁で意味が決まっていて、`4xx` は主にクライアント側の問題、`5xx` は主にサーバー側の問題です。

## 詳細説明

HTTP ステータスコードは、ブラウザやアプリがサーバーにリクエストしたあと、「結果がどうだったか」を返す番号です。たとえば成功なら `200 OK`、別の場所へ移動したなら `301` や `302`、問題があったなら `4xx` や `5xx` が返ります。

### 403 Forbidden

`403 Forbidden` は、「サーバーはリクエストの意味は分かったけど、その処理を拒否した」という状態です。

よくある例はこんな感じです。

- ログインはしているけど権限が足りない
- 特定 IP や地域からのアクセスを弾いている
- bot やスクレイピング対策で拒否されている
- ファイルやディレクトリの公開設定が禁止になっている

似た番号と混ざりやすいので、ここは切り分けが大事です。

### 401 Unauthorized

`401 Unauthorized` は、認証が必要なのに、まだ認証できていないときに返りやすいコードです。

- ログインしていない
- API キーやトークンがない
- トークン期限切れ

ざっくり言うと、

- `401`: まず認証して
- `403`: 認証していても許可されない

という違いで覚えると分かりやすいです。

### 404 Not Found

`404 Not Found` は、指定された URL の場所に目的のページやデータが見つからない状態です。

- URL の打ち間違い
- ページが削除された
- API のパスが違う

これは「存在しない」寄りのエラーで、`403` の「あるかもしれないけど見せない」とは少し意味が違います。

### 400 Bad Request

`400 Bad Request` は、リクエストの形式や内容がおかしいときに返ります。

- パラメータが不足している
- JSON の形式が壊れている
- サーバーが受け付けない値を送っている

### 500 Internal Server Error

`500 Internal Server Error` は、サーバー側で予期しない問題が起きて処理できなかったときの代表的なエラーです。

- サーバーのコードが落ちた
- DB 接続に失敗した
- 設定ミスや例外で止まった

これは基本的に利用者側より、サービス側の不具合であることが多いです。

### よくある見分け方

- `200`: うまくいった
- `301` / `302`: 別の URL へ移動
- `400`: リクエストの形がおかしい
- `401`: 認証が足りない
- `403`: 認証していても許可されない
- `404`: その場所が見つからない
- `500`: サーバー内部で失敗
- `502` / `503` / `504`: 上流サーバーや一時的障害、混雑、タイムアウト系

### 困ったときの見方

もし自分が利用者側なら、まずは次を確認すると切り分けしやすいです。

- URL が合っているか
- ログイン状態や権限が合っているか
- API キーやトークンが有効か
- しばらく待って再試行しても同じか

もし自分が作る側なら、サーバーログ、認証設定、CORS、権限設定、リバースプロキシ設定を見るのが近道です。

## 参照URL

- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/403
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Status
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/404
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Status/500
